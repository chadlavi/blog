language: ruby
cache: bundler 
before_install: gem update bundler
install:
  - bundle install
script:
  - "./build"
github_token: $GITHUB_TOKEN
keep_history: true
skip_cleanup: true
provider: pages
branches:
  only:
  - master
sudo: false
