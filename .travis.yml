language: ruby
cache: bundler 
before_install: gem update bundler
install:
  - bundle install
script:
  - "./build"
after_success:
  - "./travis/git"
github_token: $GITHUB_TOKEN
branches:
  only:
  - master
sudo: false
